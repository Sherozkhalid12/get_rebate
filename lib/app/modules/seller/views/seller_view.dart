import 'package:flutter/material.dart';
import 'package:getrebate/app/modules/property_listings/views/property_listings_view.dart';

// SellerView shows the property listings page for managing sell properties
class SellerView extends StatelessWidget {
  const SellerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const PropertyListingsView();
  }
}
